# ClipLens 运行时依赖
# GUI
PySide6>=6.6

# 图像处理
Pillow>=10.0

# AI 推理（Chinese-CLIP 通过 GitHub 安装，见下方注释）
torch>=2.0
torchvision>=0.15
onnxruntime>=1.16

# 数据库
lancedb>=0.4

# 文件安全（系统回收站）
send2trash>=1.8

# 系统监控（内存占用显示）
psutil>=5.9

# ---- Chinese-CLIP 多模态模型 ----
# 非标准 PyPI 包，需从官方仓库安装：
#   pip install cn_clip
# 或
#   pip install "cn_clip @ git+https://github.com/OFA-Sys/Chinese-CLIP.git"
# 注：cn_clip 依赖 torch / torchvision，且需与其 CUDA 版本匹配。
